[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-eval-toolkit"
version = "0.1.0"
description = "Simple AI evaluation toolkit"
authors = [{name = "Rafael Marin"}]
